project_id = "qmagnwxeijctkksqbcqz"
[functions]
# ignore_patterns is not a valid top-level key under [functions] in recent CLI versions or requires specific structure.
# Removing it for now to rely on .slugignore or default behavior.
